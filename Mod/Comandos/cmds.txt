const url = 'https://raw.githubusercontent.com/Mikill73/AnimeFireMod/main/Mod/Comandos/';

fetch(url)
  .then(response => response.text())
  .then(data => {
    // Dividir o conteúdo em linhas para obter os nomes dos arquivos
    const lines = data.split('\n');
    const fileNames = lines.map(line => line.trim());
    alert(`Nomes dos arquivos:\n${fileNames.join('\n')}`);
  })
  .catch(error => {
    console.error('Erro:', error);
  });
